/**
 * 地图插件 
 * 	TODO：要不要默认集成google地图插件里，这是个问题!
 * 
 */
justepApp.addPlugin(function() {
    if (typeof justepApp.map == "undefined"){
    	var Map = function () {
			
		};

		Map.prototype.show = function(positions) {
			
		};
		justepApp.map = new Map();
    } 
});